/*
 * reveal-fallback.ts — STUB (rempli par l'agent « ambiances + effets »).
 *
 * Contiendra le fallback IntersectionObserver (~30 lignes) pour les navigateurs
 * sans scroll-driven animations : observe les `.reveal`, ajoute `.est-visible`
 * quand l'élément entre dans le viewport. Actif uniquement si
 * `CSS.supports('animation-timeline: view()')` est faux et hors reduced-motion.
 *
 * Importé par BaseLayout (<script>) — vide pour l'instant afin que le build compile.
 */

export {};
